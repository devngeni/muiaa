import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const StyledMyOrderPageContainer = styled(Box)(() => ({
  width: "100%",
  padding: "30px",
  display: "flex",
  flexDirection: "row",
  gap: "10px",
}));

export const StyledOrderSectionWrapper = styled(Box)(() => ({
  width: "100%",
  padding: "45px",
}));
