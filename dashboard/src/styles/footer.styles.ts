import { Box, styled } from '@mui/material';

export const FooterContainer = styled(Box)(() => ({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  background: '#14263E',
  color: '#FFFFFF',
  padding: '10px 20px',
  height: '89px'
}));
